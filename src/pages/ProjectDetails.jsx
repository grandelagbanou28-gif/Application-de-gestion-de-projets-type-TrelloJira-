import { useState, useEffect } from "react" import { useSelector } from "react-redux" import { useNavigate, useSearchParams } from "react-router-dom" import { ArrowLeft, Plus, Settings, BarChart3, Calendar, ListTodo, Zap, Columns, GanttChart, Timer } from "lucide-react" import { useTranslation } from "../i18n" import ProjectAnalytics from "../components/ProjectAnalytics" import ProjectSettings from "../components/ProjectSettings" import CreateTaskDialog from "../components/CreateTaskDialog" import ProjectCalendar from "../components/ProjectCalendar" import ProjectTasks from "../components/ProjectTasks" import KanbanBoard from "../components/KanbanBoard" import SprintPlanning from "../components/SprintPlanning" import ProjectTimeline from "../components/ProjectTimeline"  export default function ProjectDetail() {   const { t } = useTranslation()   const [searchParams, setSearchParams] = useSearchParams()   const tab = searchParams.get('tab')   const id = searchParams.get('id')    const navigate = useNavigate()   const projects = useSelector((state) => state?.workspace?.currentWorkspace?.projects || [])    const [project, setProject] = useState(null)   const [tasks, setTasks] = useState([])   const [showCreateTask, setShowCreateTask] = useState(searchParams.get('createTask') === 'true')   const [activeTab, setActiveTab] = useState(tab || "tasks")    useEffect(() => {     if (tab) setActiveTab(tab)   }, [tab])    useEffect(() => {     if (projects && projects.length > 0) {       const proj = projects.find((p) => p.id === id)       setProject(proj)       setTasks(proj?.tasks || [])     }   }, [id, projects])    const statusColors = {     PLANNING: "bg-surface-100 text-surface-700 dark:bg-surface-800 dark:text-surface-300",     ACTIVE: "bg-emerald-100 text-emerald-700 dark:bg-emerald-500/20 dark:text-emerald-300",     ON_HOLD: "bg-amber-100 text-amber-700 dark:bg-amber-500/20 dark:text-amber-300",     COMPLETED: "bg-primary-100 text-primary-700 dark:bg-primary-500/20 dark:text-primary-300",     CANCELLED: "bg-red-100 text-red-700 dark:bg-red-500/20 dark:text-red-300",   }    const tabs = [     { key: "tasks", label: t('projectDetail.tasks'), icon: ListTodo },     { key: "board", label: "Board", icon: Columns },     { key: "sprints", label: "Sprints", icon: Timer },     { key: "timeline", label: "Timeline", icon: GanttChart },     { key: "calendar", label: t('projectDetail.calendar'), icon: Calendar },     { key: "analytics", label: t('projectDetail.analytics'), icon: BarChart3 },     { key: "settings", label: t('projectDetail.settings'), icon: Settings },   ]    if (!project) {     return (       <div className="flex flex-col items-center justify-center min-h-[60vh]">         <div className="w-20 h-20 rounded-2xl bg-surface-100 dark:bg-surface-800 flex items-center justify-center mb-6">           <Zap size={32} className="text-surface-400" />         </div>         <p className="text-xl font-semibold text-surface-900 dark:text-surface-100 mb-2">{t('projectDetail.projectNotFound')}</p>         <p className="text-sm text-surface-400 dark:text-surface-500 mb-6">{t('projectDetail.notFoundDesc')}</p>         <button onClick={() => navigate('/projects')} className="px-5 py-2.5 text-sm font-medium rounded-xl text-white bg-gradient-to-br from-primary-600 to-primary-700 hover:from-primary-500 hover:to-primary-600 transition-all">           {t('projectDetail.backToProjects')}         </button>       </div>     )   }    return (     <div className="max-w-7xl mx-auto space-y-6">       <div className="flex flex-col sm:flex-row items-start sm:items-center gap-4">         <div className="flex items-center gap-3 flex-1">           <button onClick={() => navigate('/projects')} className="p-2 rounded-xl text-surface-400 hover:text-surface-600 dark:hover:text-surface-200 hover:bg-surface-100 dark:hover:bg-surface-800 transition-colors">             <ArrowLeft size={18} />           </button>           <div className="flex items-center gap-3">             <h1 className="text-xl font-semibold text-surface-900 dark:text-surface-100">{project.name}</h1>             <span className={`shrink-0 text-xs font-medium px-2.5 py-0.5 rounded-full ${statusColors[project.status] || statusColors.PLANNING}`}>               {project.status.replace("_", " ").replace(/\b\w/g, c => c.toUpperCase())}             </span>           </div>         </div>         <button onClick={() => setShowCreateTask(true)} className="shrink-0 inline-flex items-center gap-2 px-5 py-2.5 text-sm font-medium rounded-xl text-white bg-gradient-to-br from-primary-600 to-primary-700 hover:from-primary-500 hover:to-primary-600 shadow-lg shadow-primary-500/20 transition-all active:scale-[0.97]">           <Plus size={16} strokeWidth={2.5} />           {t('projectDetail.newTask')}         </button>       </div>        <div className="grid grid-cols-2 sm:grid-cols-4 gap-4">         {[           { label: t('projectDetail.totalTasks'), value: tasks.length, color: "text-surface-900 dark:text-surface-100", icon: ListTodo },           { label: t('projectDetail.completed'), value: tasks.filter((t) => t.status === "DONE").length, color: "text-emerald-600 dark:text-emerald-400", icon: Zap },           { label: t('projectDetail.inProgress'), value: tasks.filter((t) => t.status === "IN_PROGRESS" || t.status === "TODO").length, color: "text-amber-600 dark:text-amber-400", icon: Zap },           { label: t('projectDetail.teamMembers'), value: project.members?.length || 0, color: "text-primary-600 dark:text-primary-400", icon: Zap },         ].map((card, idx) => (           <div key={idx} className="bg-white dark:bg-surface-900 rounded-2xl border border-surface-200 dark:border-surface-800 p-5">             <div className="flex items-center justify-between">               <div>                 <p className="text-sm text-surface-500 dark:text-surface-400 mb-0.5">{card.label}</p>                 <p className={`text-2xl font-bold tracking-tight ${card.color}`}>{card.value}</p>               </div>               <div className="p-2.5 rounded-xl bg-surface-100 dark:bg-surface-800">                 <card.icon size={16} className={card.color} />               </div>             </div>           </div>         ))}       </div>        <div className="bg-white dark:bg-surface-900 rounded-2xl border border-surface-200 dark:border-surface-800 overflow-hidden">         <div className="border-b border-surface-200 dark:border-surface-800">           <div className="flex">             {tabs.map((tabItem) => {               const Icon = tabItem.icon               return (                 <button                   key={tabItem.key}                   onClick={() => { setActiveTab(tabItem.key); setSearchParams({ id: id, tab: tabItem.key }) }}                   className={`flex items-center gap-2 px-5 py-3.5 text-sm font-medium transition-all border-b-2 ${                     activeTab === tabItem.key                       ? "border-primary-500 text-primary-700 dark:text-primary-300 bg-primary-50/50 dark:bg-primary-500/5"                       : "border-transparent text-surface-500 dark:text-surface-400 hover:text-surface-700 dark:hover:text-surface-200 hover:bg-surface-50 dark:hover:bg-surface-850"                   }`}                 >                   <Icon size={16} strokeWidth={1.5} />                   {tabItem.label}                 </button>               )             })}           </div>         </div>          <div className="p-6">           {activeTab === "tasks" && <ProjectTasks tasks={tasks} />}           {activeTab === "board" && <KanbanBoard tasks={tasks} projectId={id} />}           {activeTab === "sprints" && <SprintPlanning tasks={tasks} project={project} />}           {activeTab === "timeline" && <ProjectTimeline tasks={tasks} />}           {activeTab === "analytics" && <ProjectAnalytics tasks={tasks} project={project} />}           {activeTab === "calendar" && <ProjectCalendar tasks={tasks} />}           {activeTab === "settings" && <ProjectSettings project={project} />}         </div>       </div>        {showCreateTask && <CreateTaskDialog showCreateTask={showCreateTask} setShowCreateTask={setShowCreateTask} projectId={id} />}     </div>   ) }