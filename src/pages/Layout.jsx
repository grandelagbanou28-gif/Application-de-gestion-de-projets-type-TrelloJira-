import { useState, useEffect } from 'react' import { MessageCircle } from 'lucide-react' import Navbar from '../components/Navbar' import Sidebar from '../components/Sidebar' import ChatPanel from '../components/ChatPanel' import { Outlet, useLocation } from 'react-router-dom' import { useDispatch } from 'react-redux' import { loadTheme } from '../features/themeSlice'  const Layout = () => {   const location = useLocation()   const [isSidebarOpen, setIsSidebarOpen] = useState(false)   const [isChatOpen, setIsChatOpen] = useState(false)   const dispatch = useDispatch()    useEffect(() => {     dispatch(loadTheme())   }, [])    return (     <div className="flex bg-surface-50 dark:bg-surface-950 text-surface-900 dark:text-surface-100 antialiased">       <Sidebar isSidebarOpen={isSidebarOpen} setIsSidebarOpen={setIsSidebarOpen} />       <div className="flex-1 flex flex-col min-h-screen">         <Navbar isSidebarOpen={isSidebarOpen} setIsSidebarOpen={setIsSidebarOpen} onChatToggle={() => setIsChatOpen((p) => !p)} />         <main className="flex-1 p-6 lg:p-8 xl:p-10 2xl:px-16 overflow-y-auto">           <div key={location.pathname} className="animate-fadeIn">             <Outlet />           </div>         </main>       </div>        <button         onClick={() => setIsChatOpen((p) => !p)}         className="fixed bottom-6 right-6 z-40 size-14 rounded-2xl bg-gradient-to-br from-primary-500 to-primary-700 text-white shadow-2xl shadow-primary-500/40 hover:shadow-primary-500/60 hover:scale-105 active:scale-95 transition-all flex items-center justify-center"       >         <MessageCircle size={24} />       </button>        <ChatPanel isOpen={isChatOpen} onClose={() => setIsChatOpen(false)} />     </div>   ) }  export default Layout