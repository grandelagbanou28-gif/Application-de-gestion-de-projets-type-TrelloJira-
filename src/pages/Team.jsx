import { useEffect, useState } from "react" import { Users, Search, UserPlus, Shield, Activity, Crown } from "lucide-react" import InviteMemberDialog from "../components/InviteMemberDialog" import { useSelector } from "react-redux" import { useTranslation } from "../i18n"  const Team = () => {   const { t } = useTranslation()   const [tasks, setTasks] = useState([])   const [searchTerm, setSearchTerm] = useState("")   const [isDialogOpen, setIsDialogOpen] = useState(false)   const [users, setUsers] = useState([])   const currentWorkspace = useSelector((state) => state?.workspace?.currentWorkspace || null)   const projects = currentWorkspace?.projects || []    const filteredUsers = users.filter(     (user) =>       user?.name?.toLowerCase().includes(searchTerm.toLowerCase()) ||       user?.email?.toLowerCase().includes(searchTerm.toLowerCase())   )    useEffect(() => {     setUsers(currentWorkspace?.members || [])     setTasks(currentWorkspace?.projects?.reduce((acc, project) => [...acc, ...project.tasks], []) || [])   }, [currentWorkspace])    const statsCards = [     { label: t('team.totalMembers'), value: users.length, icon: Users, color: "text-primary-600 dark:text-primary-400", bg: "bg-primary-500/10" },     { label: t('team.activeProjects'), value: projects.filter((p) => p.status !== "CANCELLED" && p.status !== "COMPLETED").length, icon: Activity, color: "text-emerald-600 dark:text-emerald-400", bg: "bg-emerald-500/10" },     { label: t('team.totalTasks'), value: tasks.length, icon: Shield, color: "text-violet-600 dark:text-violet-400", bg: "bg-violet-500/10" },   ]    return (     <div className="max-w-7xl mx-auto space-y-8">       <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">         <div>           <h1 className="text-2xl font-bold tracking-tight text-surface-900 dark:text-white">{t('team.title')}</h1>           <p className="text-surface-500 dark:text-surface-400 text-sm mt-1">{t('team.subtitle')}</p>         </div>         <button onClick={() => setIsDialogOpen(true)} className="inline-flex items-center gap-2 px-5 py-2.5 text-sm font-medium rounded-xl text-white bg-gradient-to-br from-primary-600 to-primary-700 hover:from-primary-500 hover:to-primary-600 shadow-lg shadow-primary-500/25 transition-all active:scale-[0.97]">           <UserPlus size={16} strokeWidth={2.5} />           {t('team.inviteMember')}         </button>         <InviteMemberDialog isOpen={isDialogOpen} setIsOpen={setIsDialogOpen} />       </div>        <div className="grid grid-cols-1 sm:grid-cols-3 gap-5">         {statsCards.map((card) => {           const Icon = card.icon           return (             <div key={card.label} className="bg-white dark:bg-surface-900 rounded-2xl border border-surface-200 dark:border-surface-800 p-5">               <div className="flex items-center justify-between">                 <div>                   <p className="text-sm text-surface-500 dark:text-surface-400 mb-0.5">{card.label}</p>                   <p className="text-2xl font-bold tracking-tight text-surface-900 dark:text-white">{card.value}</p>                 </div>                 <div className={`p-3 rounded-xl ${card.bg}`}>                   <Icon size={20} className={card.color} strokeWidth={1.5} />                 </div>               </div>             </div>           )         })}       </div>        <div className="relative max-w-md">         <Search size={16} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-surface-400 dark:text-surface-500" />         <input           placeholder={t('team.search')}           value={searchTerm}           onChange={(e) => setSearchTerm(e.target.value)}           className="w-full pl-10 pr-4 py-2.5 bg-surface-100 dark:bg-surface-800 border-0 rounded-xl text-sm text-surface-900 dark:text-surface-100 placeholder-surface-400 dark:placeholder-surface-500 focus:outline-none focus:ring-2 focus:ring-primary-500/30 focus:bg-white dark:focus:bg-surface-850 transition-all"         />       </div>        {filteredUsers.length === 0 ? (         <div className="text-center py-20">           <div className="w-20 h-20 mx-auto mb-6 bg-surface-100 dark:bg-surface-800 rounded-2xl flex items-center justify-center">             <Users size={40} className="text-surface-400 dark:text-surface-500" />           </div>           <h3 className="text-lg font-semibold text-surface-900 dark:text-surface-100 mb-1">             {users.length === 0 ? t('team.noMembers') : t('team.noMatch')}           </h3>           <p className="text-surface-500 dark:text-surface-400 text-sm">             {users.length === 0 ? t('team.noMembersDesc') : t('team.adjustSearch')}           </p>         </div>       ) : (         <div className="bg-white dark:bg-surface-900 rounded-2xl border border-surface-200 dark:border-surface-800 overflow-hidden">           <div className="hidden sm:block overflow-x-auto">             <table className="w-full text-sm">               <thead>                 <tr className="bg-surface-50 dark:bg-surface-850 border-b border-surface-200 dark:border-surface-800">                   <th className="text-left px-6 py-3.5 text-xs font-semibold text-surface-500 dark:text-surface-400 uppercase tracking-wider">{t('team.name')}</th>                   <th className="text-left px-6 py-3.5 text-xs font-semibold text-surface-500 dark:text-surface-400 uppercase tracking-wider">{t('team.email')}</th>                   <th className="text-left px-6 py-3.5 text-xs font-semibold text-surface-500 dark:text-surface-400 uppercase tracking-wider">{t('team.role')}</th>                 </tr>               </thead>               <tbody className="divide-y divide-surface-100 dark:divide-surface-800">                 {filteredUsers.map((member) => (                   <tr key={member.id} className="hover:bg-surface-50 dark:hover:bg-surface-850 transition-colors">                     <td className="px-6 py-3.5 whitespace-nowrap">                       <div className="flex items-center gap-3">                         <div className="size-9 rounded-full bg-gradient-to-br from-primary-400 to-primary-600 flex items-center justify-center text-white text-sm font-bold">                           {member?.name?.[0]?.toUpperCase() || member?.email?.[0]?.toUpperCase() || "?"}                         </div>                         <span className="font-medium text-surface-900 dark:text-surface-100">{member?.name || "Unknown User"}</span>                       </div>                     </td>                     <td className="px-6 py-3.5 whitespace-nowrap text-surface-500 dark:text-surface-400">{member?.email || "-"}</td>                     <td className="px-6 py-3.5 whitespace-nowrap">                       <span className={`inline-flex items-center gap-1.5 text-xs font-medium px-3 py-1 rounded-full ${                         member.role === "ADMIN"                           ? "bg-violet-100 text-violet-700 dark:bg-violet-500/20 dark:text-violet-300"                           : "bg-surface-100 text-surface-600 dark:bg-surface-800 dark:text-surface-300"                       }`}>                         {member.role === "ADMIN" && <Crown size={12} />}                         {member.role || "MEMBER"}                       </span>                     </td>                   </tr>                 ))}               </tbody>             </table>           </div>            <div className="sm:hidden divide-y divide-surface-100 dark:divide-surface-800">                 {filteredUsers.map((member) => (               <div key={member.id} className="p-4 hover:bg-surface-50 dark:hover:bg-surface-850 transition-colors">                 <div className="flex items-center gap-3 mb-2">                   <div className="size-10 rounded-full bg-gradient-to-br from-primary-400 to-primary-600 flex items-center justify-center text-white text-sm font-bold">                     {member?.name?.[0]?.toUpperCase() || member?.email?.[0]?.toUpperCase() || "?"}                   </div>                   <div>                     <p className="font-medium text-surface-900 dark:text-surface-100">{member?.name || "Unknown User"}</p>                     <p className="text-sm text-surface-400 dark:text-surface-500">{member?.email || "-"}</p>                   </div>                 </div>                 <span className={`inline-flex items-center gap-1 text-xs font-medium px-2.5 py-0.5 rounded-full ${                   member.role === "ADMIN"                     ? "bg-violet-100 text-violet-700 dark:bg-violet-500/20 dark:text-violet-300"                     : "bg-surface-100 text-surface-600 dark:bg-surface-800 dark:text-surface-300"                 }`}>                   {member.role === "ADMIN" && <Crown size={10} />}                   {member.role || "MEMBER"}                 </span>               </div>             ))}           </div>         </div>       )}     </div>   ) }  export default Team