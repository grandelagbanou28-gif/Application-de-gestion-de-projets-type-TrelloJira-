import { useSelector } from "react-redux"; import { formatDistanceToNow } from "date-fns"; import {   ListTodo,   UserPlus,   FolderKanban,   Rocket,   CheckCircle,   Clock,   Activity, } from "lucide-react";  const typeConfig = {   task_created: {     icon: ListTodo,     color: "bg-blue-500",     dot: "bg-blue-500",   },   task_updated: {     icon: ListTodo,     color: "bg-amber-500",     dot: "bg-amber-500",   },   task_completed: {     icon: CheckCircle,     color: "bg-emerald-500",     dot: "bg-emerald-500",   },   member_joined: {     icon: UserPlus,     color: "bg-indigo-500",     dot: "bg-indigo-500",   },   project_created: {     icon: FolderKanban,     color: "bg-violet-500",     dot: "bg-violet-500",   },   sprint_created: {     icon: Rocket,     color: "bg-orange-500",     dot: "bg-orange-500",   },   sprint_completed: {     icon: Rocket,     color: "bg-teal-500",     dot: "bg-teal-500",   }, };  export default function ActivityLog({ activities: propActivities, maxItems }) {   const currentWorkspace = useSelector(     (state) => state.workspace.currentWorkspace   );   const activities = propActivities ?? currentWorkspace?.activities ?? [];    const displayed = maxItems ? activities.slice(0, maxItems) : activities;    if (!displayed.length) {     return (       <div className="flex flex-col items-center justify-center py-12 text-surface-500">         <Clock className="h-10 w-10 mb-3" />         <p className="text-sm font-medium">No recent activity</p>       </div>     );   }    return (     <div className="space-y-1">       {displayed.map((item) => {         const config = typeConfig[item.type] || typeConfig.task_created;         const Icon = config.icon;          return (           <div             key={item.id}             className="flex items-start gap-3 rounded-xl px-4 py-3 transition-colors hover:bg-surface-100/50"           >             <div className="relative mt-0.5 flex-shrink-0">               <div                 className={`flex h-8 w-8 items-center justify-center rounded-lg ${config.color}/10`}               >                 <Icon className={`h-4 w-4 ${config.color.replace("bg-", "text-")}`} />               </div>               <span                 className={`absolute -bottom-0.5 -right-0.5 h-2.5 w-2.5 rounded-full border-2 border-surface-0 ${config.dot}`}               />             </div>              <div className="min-w-0 flex-1">               <p className="text-sm text-surface-700">                 <span className="font-medium text-surface-900">                   {item.userName}                 </span>{" "}                 {item.message}               </p>               <p className="mt-0.5 text-xs text-surface-500">                 {formatDistanceToNow(new Date(item.createdAt), {                   addSuffix: true,                 })}               </p>             </div>           </div>         );       })}     </div>   ); }