import { useMemo } from "react" import { useTranslation } from "../i18n" import { Calendar, GanttChart } from "lucide-react" import { format, differenceInDays, startOfWeek, addDays } from "date-fns"  const priorityColors = {   LOW: "bg-surface-200 dark:bg-surface-700",   MEDIUM: "bg-amber-400",   HIGH: "bg-orange-500",   CRITICAL: "bg-red-500", }  export default function ProjectTimeline({ tasks }) {   const { t } = useTranslation()    const sorted = useMemo(() => {     return [...tasks]       .filter((t) => t.dueDate)       .sort((a, b) => new Date(a.dueDate) - new Date(b.dueDate))   }, [tasks])    if (sorted.length === 0) {     return (       <div className="text-center py-12">         <GanttChart size={40} className="mx-auto mb-3 text-surface-300 dark:text-surface-600" />         <p className="text-surface-500 dark:text-surface-400 text-sm">{t('common.noData') || "No tasks with due dates to display on timeline."}</p>       </div>     )   }    const dates = sorted.map((t) => new Date(t.dueDate))   const minDate = dates.length > 0 ? startOfWeek(dates[0]) : new Date()   const maxDate = dates.length > 0 ? dates[dates.length - 1] : new Date()   const dayCount = differenceInDays(maxDate, minDate) + 1 || 7   const dayWidth = 32   const totalWidth = Math.max(dayCount * dayWidth, 400)    const days = Array.from({ length: Math.min(dayCount, 60) }, (_, i) => addDays(minDate, i))    return (     <div className="space-y-4">       <h2 className="text-lg font-semibold text-surface-900 dark:text-surface-100 flex items-center gap-2">         <GanttChart size={18} /> Timeline       </h2>        <div className="overflow-x-auto bg-white dark:bg-surface-900 rounded-2xl border border-surface-200 dark:border-surface-800">         <div className="min-w-[600px]" style={{ width: `${totalWidth + 280}px` }}>           <div className="flex border-b border-surface-200 dark:border-surface-800">             <div className="w-[240px] shrink-0 p-3 text-xs font-medium text-surface-500 dark:text-surface-400 uppercase tracking-wider">               Task             </div>             <div className="flex flex-1">               {days.map((day, i) => (                 <div                   key={i}                   className="shrink-0 p-2 text-center text-xs text-surface-400 dark:text-surface-500 border-l border-surface-100 dark:border-surface-800"                   style={{ width: `${dayWidth}px` }}                 >                   {format(day, "d")}                   <div className="text-[10px] leading-none mt-0.5">{format(day, "EEE")}</div>                 </div>               ))}             </div>           </div>            {sorted.slice(0, 20).map((task) => {             const taskDate = new Date(task.dueDate)             const offsetDays = differenceInDays(taskDate, minDate)             const left = offsetDays * dayWidth             const barWidth = Math.max(dayWidth * 1.5, dayWidth)             const isOverdue = taskDate < new Date() && task.status !== "DONE"              return (               <div key={task.id} className="flex border-b border-surface-100 dark:border-surface-800 last:border-b-0">                 <div className="w-[240px] shrink-0 p-3 flex items-center gap-2">                   <div className={`w-1.5 h-1.5 rounded-full ${priorityColors[task.priority] || priorityColors.MEDIUM}`} />                   <span className="text-sm text-surface-700 dark:text-surface-300 truncate">{task.title}</span>                 </div>                 <div className="flex-1 relative p-2 min-h-[36px]">                   <div                     className={`absolute top-2 h-5 rounded flex items-center px-1.5 ${                       isOverdue                         ? "bg-red-100 dark:bg-red-500/20"                         : task.status === "DONE"                           ? "bg-emerald-100 dark:bg-emerald-500/20"                           : "bg-primary-100 dark:bg-primary-500/20"                     }`}                     style={{ left: `${left}px`, width: `${barWidth}px` }}                   >                     <span className="text-[10px] leading-none font-medium truncate text-surface-600 dark:text-surface-300">                       {format(taskDate, "MMM d")}                     </span>                   </div>                 </div>               </div>             )           })}         </div>       </div>     </div>   ) }