import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { ArrowRight, Calendar, Users, FolderOpen, BarChart3, X } from "lucide-react";
import { format } from "date-fns";
import { useSelector } from "react-redux";
import { useTranslation } from "../i18n";
import CreateProjectDialog from "./CreateProjectDialog";
const ProjectOverview = () => {
  const {
    t
  } = useTranslation();
  const statusColors = {
    PLANNING: "bg-surface-100 text-surface-700 dark:bg-surface-800 dark:text-surface-300",
    ACTIVE: "bg-emerald-100 text-emerald-700 dark:bg-emerald-500/20 dark:text-emerald-300",
    ON_HOLD: "bg-amber-100 text-amber-700 dark:bg-amber-500/20 dark:text-amber-300",
    COMPLETED: "bg-primary-100 text-primary-700 dark:bg-primary-500/20 dark:text-primary-300",
    CANCELLED: "bg-red-100 text-red-700 dark:bg-red-500/20 dark:text-red-300"
  };
  const currentWorkspace = useSelector(state => state?.workspace?.currentWorkspace || null);
  const [isDialogOpen, setIsDialogOpen] = useState(false);
  const [projects, setProjects] = useState([]);
  useEffect(() => {
    setProjects(currentWorkspace?.projects || []);
  }, [currentWorkspace]);
  return currentWorkspace && <div className="bg-white dark:bg-surface-900 rounded-2xl border border-surface-200 dark:border-surface-800 overflow-hidden">       <div className="px-6 py-4 border-b border-surface-200 dark:border-surface-800 flex items-center justify-between">         <h2 className="text-base font-semibold text-surface-900 dark:text-surface-100">{t('dashboard.activeProjects')}</h2>         <Link to="/projects" className="inline-flex items-center gap-1.5 text-sm font-medium text-primary-600 dark:text-primary-400 hover:text-primary-700 dark:hover:text-primary-300 transition-colors">           {t('sidebar.viewAll')} <ArrowRight size={14} />         </Link>       </div>        {projects.length === 0 ? <div className="py-16 text-center">           <div className="w-16 h-16 mx-auto mb-4 bg-surface-100 dark:bg-surface-800 rounded-2xl flex items-center justify-center">             <FolderOpen size={28} className="text-surface-400 dark:text-surface-500" />           </div>           <p className="text-surface-600 dark:text-surface-400 font-medium mb-1">{t('dashboard.noProjects')}</p>           <p className="text-sm text-surface-400 dark:text-surface-500 mb-4">{t('dashboard.noProjectsDesc')}</p>           <button onClick={() => setIsDialogOpen(true)} className="inline-flex items-center gap-2 px-5 py-2.5 text-sm font-medium rounded-xl text-white bg-gradient-to-br from-primary-600 to-primary-700 hover:from-primary-500 hover:to-primary-600 shadow-lg shadow-primary-500/20 transition-all">             {t('dashboard.createFirst')}           </button>           <CreateProjectDialog isDialogOpen={isDialogOpen} setIsDialogOpen={setIsDialogOpen} />         </div> : <div className="divide-y divide-surface-100 dark:divide-surface-800">           {projects.slice(0, 5).map(project => <Link key={project.id} to={`/projectsDetail?id=${project.id}&tab=tasks`} className="block px-6 py-5 hover:bg-surface-50 dark:hover:bg-surface-850 transition-colors group">               <div className="flex items-start justify-between mb-3">                 <div className="flex-1 min-w-0">                   <div className="flex items-center gap-2 mb-1">                     <h3 className="font-semibold text-surface-900 dark:text-surface-100 truncate group-hover:text-primary-600 dark:group-hover:text-primary-400 transition-colors">                       {project.name}                     </h3>                     <span className={`shrink-0 text-[11px] font-medium px-2 py-0.5 rounded-full ${statusColors[project.status] || statusColors.PLANNING}`}>                       {project.status.replace("_", " ").replace(/\b\w/g, c => c.toUpperCase())}                     </span>                   </div>                   <p className="text-sm text-surface-500 dark:text-surface-400 line-clamp-1">                     {project.description || t('common.noDescription')}                   </p>                 </div>               </div>                <div className="flex items-center gap-4 text-xs text-surface-400 dark:text-surface-500 mb-3">                 {project.members?.length > 0 && <div className="flex items-center gap-1">                     <Users size={12} />                     {project.members.length} {t('settings.members')}                   </div>}                 {project.end_date && <div className="flex items-center gap-1">                     <Calendar size={12} />                     {format(new Date(project.end_date), "MMM d, yyyy")}                   </div>}                 <div className="flex items-center gap-1">                   <BarChart3 size={12} />                   {project.tasks?.length || 0} {t('projectDetail.tasks').toLowerCase()}                 </div>               </div>                <div className="space-y-1.5">                 <div className="flex items-center justify-between text-xs">                   <span className="text-surface-400 dark:text-surface-500">{t('common.progress')}</span>                   <span className="font-medium text-surface-600 dark:text-surface-400">{project.progress || 0}%</span>                 </div>                 <div className="w-full h-2 bg-surface-100 dark:bg-surface-800 rounded-full overflow-hidden">                   <div className="h-full bg-gradient-to-r from-primary-500 to-primary-600 rounded-full transition-all duration-500" style={{
              width: `${project.progress || 0}%`
            }} />                 </div>               </div>             </Link>)}         </div>}     </div>;
};
export default ProjectOverview;