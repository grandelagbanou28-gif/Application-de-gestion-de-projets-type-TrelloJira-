import { createSlice } from "@reduxjs/toolkit"  function saveState(state) {   try {     localStorage.setItem("sprintboard_workspaces", JSON.stringify(state.workspaces))     localStorage.setItem("sprintboard_currentWorkspaceId", state.currentWorkspace?.id || "")   } catch {} }  const saved = localStorage.getItem("sprintboard_workspaces") const savedWorkspaces = saved ? JSON.parse(saved) : [] const savedId = localStorage.getItem("sprintboard_currentWorkspaceId")  const initialState = {   workspaces: savedWorkspaces,   currentWorkspace: savedWorkspaces.find((w) => w.id === savedId) || null,   loading: false, }  const workspaceSlice = createSlice({   name: "workspace",   initialState,   reducers: {     setWorkspaces: (state, action) => {       state.workspaces = action.payload       saveState(state)     },     setCurrentWorkspace: (state, action) => {       state.currentWorkspace = state.workspaces.find((w) => w.id === action.payload)       saveState(state)     },     addWorkspace: (state, action) => {       state.workspaces.push(action.payload)       if (state.currentWorkspace?.id !== action.payload.id) {         state.currentWorkspace = action.payload       }       saveState(state)     },     updateWorkspace: (state, action) => {       state.workspaces = state.workspaces.map((w) =>         w.id === action.payload.id ? action.payload : w       )       if (state.currentWorkspace?.id === action.payload.id) {         state.currentWorkspace = action.payload       }       saveState(state)     },     deleteWorkspace: (state, action) => {       state.workspaces = state.workspaces.filter((w) => w.id !== action.payload)       if (state.currentWorkspace?.id === action.payload) state.currentWorkspace = null       saveState(state)     },     addProject: (state, action) => {       const project = action.payload       state.currentWorkspace.projects.push(project)       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? { ...w, projects: [...(w.projects || []), project] }           : w       )       saveState(state)     },     addTask: (state, action) => {       const task = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === task.projectId ? { ...p, tasks: [...(p.tasks || []), task] } : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === task.projectId ? { ...p, tasks: [...(p.tasks || []), task] } : p               ),             }           : w       )       saveState(state)     },     updateTask: (state, action) => {       const updated = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === updated.projectId           ? { ...p, tasks: (p.tasks || []).map((t) => (t.id === updated.id ? updated : t)) }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === updated.projectId                   ? { ...p, tasks: (p.tasks || []).map((t) => (t.id === updated.id ? updated : t)) }                   : p               ),             }           : w       )       saveState(state)     },     deleteTask: (state, action) => {       const { ids, projectId } = action.payload       const idSet = new Set(ids)       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId ? { ...p, tasks: (p.tasks || []).filter((t) => !idSet.has(t.id)) } : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, tasks: (p.tasks || []).filter((t) => !idSet.has(t.id)) }                   : p               ),             }           : w       )       saveState(state)     },     addMember: (state, action) => {       const member = action.payload       state.currentWorkspace.members.push(member)       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? { ...w, members: [...(w.members || []), member] }           : w       )       saveState(state)     },     addProjectMember: (state, action) => {       const { projectId, member } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId ? { ...p, members: [...(p.members || []), member] } : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId ? { ...p, members: [...(p.members || []), member] } : p               ),             }           : w       )       saveState(state)     },     updateProject: (state, action) => {       const updated = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === updated.id ? updated : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? { ...w, projects: w.projects.map((p) => (p.id === updated.id ? updated : p)) }           : w       )       saveState(state)     },     addSprint: (state, action) => {       const { projectId, sprint } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, sprints: [...(p.sprints || []), sprint] }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, sprints: [...(p.sprints || []), sprint] }                   : p               ),             }           : w       )       saveState(state)     },     updateSprint: (state, action) => {       const { projectId, sprint } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, sprints: (p.sprints || []).map((s) => (s.id === sprint.id ? sprint : s)) }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, sprints: (p.sprints || []).map((s) => (s.id === sprint.id ? sprint : s)) }                   : p               ),             }           : w       )       saveState(state)     },     addEpic: (state, action) => {       const { projectId, epic } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, epics: [...(p.epics || []), epic] }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, epics: [...(p.epics || []), epic] }                   : p               ),             }           : w       )       saveState(state)     },     updateEpic: (state, action) => {       const { projectId, epic } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, epics: (p.epics || []).map((e) => (e.id === epic.id ? epic : e)) }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, epics: (p.epics || []).map((e) => (e.id === epic.id ? epic : e)) }                   : p               ),             }           : w       )       saveState(state)     },     deleteEpic: (state, action) => {       const { projectId, epicId } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, epics: (p.epics || []).filter((e) => e.id !== epicId) }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, epics: (p.epics || []).filter((e) => e.id !== epicId) }                   : p               ),             }           : w       )       saveState(state)     },     addUserStory: (state, action) => {       const { projectId, userStory } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, userStories: [...(p.userStories || []), userStory] }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, userStories: [...(p.userStories || []), userStory] }                   : p               ),             }           : w       )       saveState(state)     },     updateUserStory: (state, action) => {       const { projectId, userStory } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, userStories: (p.userStories || []).map((us) => (us.id === userStory.id ? userStory : us)) }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, userStories: (p.userStories || []).map((us) => (us.id === userStory.id ? userStory : us)) }                   : p               ),             }           : w       )       saveState(state)     },     deleteUserStory: (state, action) => {       const { projectId, userStoryId } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, userStories: (p.userStories || []).filter((us) => us.id !== userStoryId) }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, userStories: (p.userStories || []).filter((us) => us.id !== userStoryId) }                   : p               ),             }           : w       )       saveState(state)     },     addSubtask: (state, action) => {       const { projectId, taskId, subtask } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? {               ...p,               tasks: (p.tasks || []).map((t) =>                 t.id === taskId                   ? { ...t, subtasks: [...(t.subtasks || []), subtask] }                   : t               ),             }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? {                       ...p,                       tasks: (p.tasks || []).map((t) =>                         t.id === taskId                           ? { ...t, subtasks: [...(t.subtasks || []), subtask] }                           : t                       ),                     }                   : p               ),             }           : w       )       saveState(state)     },     updateSubtask: (state, action) => {       const { projectId, taskId, subtask } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? {               ...p,               tasks: (p.tasks || []).map((t) =>                 t.id === taskId                   ? { ...t, subtasks: (t.subtasks || []).map((s) => (s.id === subtask.id ? subtask : s)) }                   : t               ),             }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? {                       ...p,                       tasks: (p.tasks || []).map((t) =>                         t.id === taskId                           ? { ...t, subtasks: (t.subtasks || []).map((s) => (s.id === subtask.id ? subtask : s)) }                           : t                       ),                     }                   : p               ),             }           : w       )       saveState(state)     },     deleteSubtask: (state, action) => {       const { projectId, taskId, subtaskId } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? {               ...p,               tasks: (p.tasks || []).map((t) =>                 t.id === taskId                   ? { ...t, subtasks: (t.subtasks || []).filter((s) => s.id !== subtaskId) }                   : t               ),             }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? {                       ...p,                       tasks: (p.tasks || []).map((t) =>                         t.id === taskId                           ? { ...t, subtasks: (t.subtasks || []).filter((s) => s.id !== subtaskId) }                           : t                       ),                     }                   : p               ),             }           : w       )       saveState(state)     },     addAttachment: (state, action) => {       const { projectId, taskId, attachment } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? {               ...p,               tasks: (p.tasks || []).map((t) =>                 t.id === taskId                   ? { ...t, attachments: [...(t.attachments || []), attachment] }                   : t               ),             }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? {                       ...p,                       tasks: (p.tasks || []).map((t) =>                         t.id === taskId                           ? { ...t, attachments: [...(t.attachments || []), attachment] }                           : t                       ),                     }                   : p               ),             }           : w       )       saveState(state)     },     deleteAttachment: (state, action) => {       const { projectId, taskId, attachmentId } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? {               ...p,               tasks: (p.tasks || []).map((t) =>                 t.id === taskId                   ? { ...t, attachments: (t.attachments || []).filter((a) => a.id !== attachmentId) }                   : t               ),             }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? {                       ...p,                       tasks: (p.tasks || []).map((t) =>                         t.id === taskId                           ? { ...t, attachments: (t.attachments || []).filter((a) => a.id !== attachmentId) }                           : t                       ),                     }                   : p               ),             }           : w       )       saveState(state)     },     addComment: (state, action) => {       const { projectId, taskId, comment } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? {               ...p,               tasks: (p.tasks || []).map((t) =>                 t.id === taskId                   ? { ...t, comments: [...(t.comments || []), comment] }                   : t               ),             }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? {                       ...p,                       tasks: (p.tasks || []).map((t) =>                         t.id === taskId                           ? { ...t, comments: [...(t.comments || []), comment] }                           : t                       ),                     }                   : p               ),             }           : w       )       saveState(state)     },     addActivity: (state, action) => {       const activity = action.payload       if (!state.currentWorkspace.activities) {         state.currentWorkspace.activities = []       }       state.currentWorkspace.activities.push(activity)       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? { ...w, activities: [...(w.activities || []), activity] }           : w       )       saveState(state)     },     updateGithub: (state, action) => {       const { projectId, github } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? { ...p, github }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? { ...p, github }                   : p               ),             }           : w       )       saveState(state)     },     toggleBacklog: (state, action) => {       const { projectId, taskId } = action.payload       state.currentWorkspace.projects = state.currentWorkspace.projects.map((p) =>         p.id === projectId           ? {               ...p,               tasks: (p.tasks || []).map((t) =>                 t.id === taskId                   ? { ...t, inBacklog: !t.inBacklog }                   : t               ),             }           : p       )       state.workspaces = state.workspaces.map((w) =>         w.id === state.currentWorkspace.id           ? {               ...w,               projects: w.projects.map((p) =>                 p.id === projectId                   ? {                       ...p,                       tasks: (p.tasks || []).map((t) =>                         t.id === taskId                           ? { ...t, inBacklog: !t.inBacklog }                           : t                       ),                     }                   : p               ),             }           : w       )       saveState(state)     },   }, })  export const {   setWorkspaces,   setCurrentWorkspace,   addWorkspace,   updateWorkspace,   deleteWorkspace,   addProject,   addTask,   updateTask,   deleteTask,   addMember,   addProjectMember,   updateProject,   addSprint,   updateSprint,   addEpic,   updateEpic,   deleteEpic,   addUserStory,   updateUserStory,   deleteUserStory,   addSubtask,   updateSubtask,   deleteSubtask,   addAttachment,   deleteAttachment,   addComment,   addActivity,   updateGithub,   toggleBacklog, } = workspaceSlice.actions export default workspaceSlice.reducer