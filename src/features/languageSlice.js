import { createSlice } from "@reduxjs/toolkit";  const storedLang = localStorage.getItem("sprintboard_lang") || "fr";  const languageSlice = createSlice({     name: "language",     initialState: {         lang: storedLang,     },     reducers: {         setLanguage: (state, action) => {             state.lang = action.payload;             localStorage.setItem("sprintboard_lang", action.payload);         },     }, });  export const { setLanguage } = languageSlice.actions; export default languageSlice.reducer;